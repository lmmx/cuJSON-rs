# 2026-09-23: cujson-bench, cuJSON vs simd-json on Wikidata claims

## Current State

- `crates/cujson-bench` (`publish = false`, workspace member) is a binary with subcommands `run` and `verify`; feature `cuda` forwards to `cujson/cuda`, and the `cpu-reference` feature of `cujson` is always on
- `data.rs` reads one string column (default `claims`) of a parquet file with the `parquet` crate, skips null, empty and raw-newline rows (counted and printed), and joins the rest as JSON Lines into batches of at most `--batch-mb` MiB
- `engines.rs` runs eight engines per batch at two levels (`parse`, `walk`): `simd`, `simd-par`, `simd-buf`, `simd-buf-par` (`simd_json::to_borrowed_value`, or `to_borrowed_value_with_buffers`, over `split_mut` or rayon `par_split_mut` rows, on a per-run mutable copy of the batch, timed as phase `copy`) and `cujson-node`, `cujson-node-par`, `cujson-visit`, `cujson-visit-par` (tape from `cujson::parse_lines`, or from `cujson::cpu::parse` with `--tape cpu`, walked by `Node` navigation over `Document::lines()`, or by `Document::visit` / `Document::visit_range` over `Document::split_lines(threads * 16)` ranges)
- `crates/cujson/src/tape/visit.rs` defines the `Visitor` trait (`begin_object`, `end_object`, `begin_array`, `end_array`, `key`, `string`, `number`, `boolean`, `null`; strings borrowed from a per-walk scratch `String`) and `Document::visit`, `Document::visit_range`, `Document::split_lines`; the walk reads `structural` and the input sequentially and never reads `pair_pos`, while `split_lines` reads `pair_pos` at line openers only; `crates/cujson/tests/visit_tests.rs` checks the event stream against `Node` navigation on edge cases, JSON Lines with blank lines and no trailing newline, both fixtures, and at 1, 2, 3, 7 and 64 ranges
- `walk.rs` folds every node into a `u64`: tag, integer-or-float bits (`as_i64`, else `as_f64`; simd-json `U64` above `i64::MAX` folded as `f64`), hash of unescaped strings and keys, array length and order, and object entries combined with wrapping addition because `simd_json` objects iterate in hash order
- `walk.rs` `HashVisitor` computes the same checksum from `Visitor` events as `walk_cujson` does from `Node`s; a unit test compares them
- `run` prints median, min and max wall time over `--reps` (default 5, after `--warmup` 2), GB/s, rows/s, per-phase medians and peak RSS above the loaded corpus (`/proc/self/status` `VmHWM`, reset through `/proc/self/clear_refs`); `--json` prints one object per engine and level; a `walk` checksum that differs from the first engine's aborts the run
- `leak` parses the first batch `--iters` times (default 30) with `--engine` and prints VmRSS, VmHWM and free device memory (`cujson::device_memory`) after each iteration
- `verify` compares per-row `walk` checksums of simd-json, `cujson::cpu::parse(.., Mode::Lines)` and, when a device is present, `cujson::parse_lines`, and exits non-zero on any mismatch
- `data/chunk_0-00283-of-00546.parquet` (92,039,060 bytes, SHA-256 `4c21bb31…9246e4`) is committed; `fetch.sh` downloads it from revision `064f404b…` of `philippesaade/wikidata`, the revision recorded in `~/lab/wikidata`'s `.cache/huggingface` metadata for the pipeline's own copies
- The file holds 1818 rows of `claims`, 233.4 MB as JSON Lines (mean 128,380 B per row, max 2,601,658 B), so one 256 MiB batch and one cuJSON chunk
- The current `main` of `philippesaade/wikidata` lists 7,449 files named `data/chunk_N.parquet` (959 GB), not the `chunk_N-XXXXX-of-XXXXX.parquet` names of the pipeline's 9,687 files, so `resolve/main/data/chunk_0-00283-of-00546.parquet` returns 404
- Two unit tests in `walk.rs` (`parsers_agree_on_edge_cases`, `checksum_detects_differences`) compare simd-json with the CPU-reference tape on numbers, escapes, nesting and key order, and check that value, key, array-order and number-type changes alter the checksum
- simd-json 0.17 returns `InvalidNumber` for the integer `123456789012345678901234567890` (beyond `u64`), where the cuJSON walker folds such a number through `as_f64`

- On the committed file the tape has 10,355,007 entries: 113,120 objects, 31,203 arrays, 5,088,241 keys (14.95 MB), 4,963,380 strings (99.9 MB, 2,843,045 of them containing a backslash escape, sampled ones being non-ASCII text as `\uXXXX`), 14,348 integers, 289 floats
- Before commit 5b9abca `unescape` parsed each `\uXXXX` with `from_utf8` and `from_str_radix` and allocated a `String` per escaped string; a `Visitor` that ignored every value took 0.613 s over the CPU-built tape, and the full `HashVisitor` 0.716 s; after it, `cujson-visit` `--tape cpu` walk takes 0.530-0.544 s and `cujson-node` 0.674-0.685 s (single thread, from 0.793 s)
- `--tape cpu` walk phase over the committed file, 20 threads: `cujson-visit-par` 0.050 s, `cujson-node-par` 0.063 s; `simd-par` `parse+walk` 0.075 s plus 0.068 s `copy`
- The `cujson-node` walk phase is 0.755 s over a GPU-built tape and 0.788 s over the CPU-built tape, before the unescape change

- `leak` on the RTX 3090 host before commit ed01209 showed RSS rising 79 MB per JSON Lines parse (351 to 2,721 MB over 30 iterations) with free device memory flat to within 4 MB; 79 MiB equals 10,355,007 tape entries times 8 bytes, the size of one tape
- `parse_json_lines.cu` copies each chunk's result into a pinned `res_buf_arrays[i]` and `mergeChunks` copies those into one buffer; before ed01209 the per-chunk buffers were freed only on the error paths, and ed01209 frees them after `mergeChunks`; `parse_standard_json.cu` makes a single pinned allocation returned to the caller and is unaffected
- After ed01209 `leak` on the RTX 3090 host reads RSS 351 MB, HWM 572 MB and free device memory 22,429 MB on all 30 iterations, and `repeated_lines_parses_do_not_grow_host_memory` in `crates/cujson/tests/gpu.rs` passes (tier 3)
- On the RTX 3090 host after ed01209 (`run` over the committed file, one process): `cujson-visit-par` walk 0.154 s (`gpu parse` 0.098 s, `walk` 0.050 s, `free` 0.006 s), `cujson-node-par` walk 0.168 s, `simd-par` walk 0.142 s (`copy` 0.069 s), `cujson-*` parse 0.107 s (`gpu parse` 0.099 s); the `+MB RSS` column reads 458 on all eight cuJSON rows and 222 to 424 on the eight simd-json rows; before ed01209 the cuJSON rows read 918 to 4,790

- `nsys profile --stats=true` of four 233 MB `cujson::parse_lines` calls (`run --engines cujson-visit-par --levels parse --reps 3 --warmup 1`, plus one `{}` probe parse) on the RTX 3090 host reports per parse: about 2 ms of kernel time (`bitMapCreatorSimd` 0.55 ms, `extractStructuralIdx` 0.37 ms, `fusedStep3_4` 0.27 ms, `checkAscii` 0.27 ms), 24.6 ms of pageable host-to-device copy for 233.4 MB, 2 `cudaHostAlloc` calls of about 17.5 ms each, 2 `cudaFreeHost` calls of about 6.5 ms each, and two device-to-host copies of 41.4 MB at about 4 ms each
- `parse_json_lines.cu` allocated a pinned buffer per chunk and a second one in `mergeChunks`, copying between them on the host; for `chunkCount == 1` it now copies both device arrays straight into the final buffer at the offsets `mergeChunks` used (`structural` at +1, `pair_pos` at +N+2) and skips the merge; the multi-chunk path is unchanged

- With commits ce7ae93 and 63a7ea2 on the RTX 3090 host (`run --engines simd-par,cujson-visit-par --levels walk`, 256 MB batch): `cujson-visit-par` 0.103 s (`gpu parse` 0.052 s, `walk` 0.051 s, `free` 0.000 s, +329 MB RSS) against `simd-par` 0.140 s; after ce7ae93 alone `gpu parse` read 0.071 s and the total 0.127 s; `leak --iters 30` reads RSS 430 MB, HWM 571 MB and free device memory 22,505 MB on all iterations; the `--include-ignored` GPU tests pass
- `cujson-pipe` (`run_pipeline` in `engines.rs`) parses batches on one thread and walks the previous batch with `Document::visit_range` in rayon through a `sync_channel(1)`; it reports wall time with `parse busy` and `walk busy` per stage, is skipped at the `parse` level, and needs `--batch-mb` below the corpus size to overlap; over the CPU-built tape at `--batch-mb 32` it reads 0.433 s wall with 0.425 s parse busy, against 0.467 s for `cujson-visit-par`
- `simd-par` `run --batch-mb` sweep on the container's 20 threads earlier read 0.110 s at 32 MB batches (`copy` 0.019 s) against 0.140 s at 256 MB

- RTX 3090 host `run --batch-mb N --engines simd-par,cujson-visit-par,cujson-pipe --levels walk` after 63a7ea2: `simd-par` 0.123 / 0.110 / 0.148 / 0.147 / 0.142 s at 16 / 32 / 64 / 128 / 256 MB; `cujson-visit-par` 0.137 / 0.113 / 0.113 / 0.106 / 0.102 s; `cujson-pipe` 0.085 / 0.080 / 0.082 / 0.092 / 0.101 s; at 32 MB `cujson-pipe` reports `parse busy` 0.068 s and `walk busy` 0.069 s against `gpu parse` 0.052 s and `walk` 0.061 s in `cujson-visit-par`

## Verification tiers

- Tier 1 (container, no `cuda`): `cargo fmt --check`, `cargo clippy -p cujson-bench --all-targets -- -D warnings`, `cargo test -p cujson-bench` pass; `verify` on the committed file reports 1818 rows with 0 mismatching checksums between simd-json and the CPU-reference tape
- Tier 1 timings on the container's 20 threads over the committed file (medians of 3): `simd` parse 0.524 s (0.45 GB/s), `simd-par` parse 0.137 s (1.71 GB/s) of which 0.069 s is the `copy` phase, `simd-par` walk 0.144 s, `simd-buf-par` parse 0.142 s
- Tier 2 (`CUJSON_CUDA_ARCHS=86`, `--features cuda`): builds and passes clippy; `run --engines cujson` prints `unavailable (CUDA error 35 ...)` and exits without panicking in the container's driverless environment
- Tier 3: no `cuJSON` timing and no GPU-tape checksum agreement has been observed; `verify` and `run` with `--features cuda` are unrun on a GPU

## Missing

- Comparison against `genson-rs`'s schema-inference walk or `polars`' `json_decode` on the same rows
- GPU memory high-water mark (only host RSS is recorded)
- A sweep over rows-per-file or multi-chunk (`--batch-mb` below 233) inputs has not been run
